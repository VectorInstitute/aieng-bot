[project]
name = "aieng-bot-slack-agent"
version = "0.1.0"
description = "aieng-bot Slack agent (Socket Mode)"
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.11.0",
    "python-dotenv>=1.0.0",
    "slack-bolt>=1.21.0",
]

[tool.uv]
package = false
